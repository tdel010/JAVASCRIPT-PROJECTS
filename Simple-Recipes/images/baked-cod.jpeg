/*Import link for the Josefin Slab font from Google Fonts*/
@import url('https://fonts.googleapis.com/css2?family=Josefin+Slab:ital,wght@0,100..700;1,100..700&display=swap');

/*Import link for the Sacramento font from fonts.google.com*/
@import url('https://fonts.googleapis.com/css2?family=Sacramento&display=swap');

html {
    overflow-x: hidden;
    /*Prevents horizontal scrolling on the page*/
    scroll-behavior: smooth;
    /*Enables smooth scrolling when navigating to different sections of the page using anchor links*/
}

/*NAVBAR*/
.nav-bar {
    position: sticky;
    /*Creates a sticky element that stays
    in the same spot in the browser screen*/
    top: 0;
    /*Tells the browser to stick the element at the
    top of the page based on user scrolling*/
    z-index: 9;
    /*Sets the stack layer of the navbar to scroll
    over all elements*/
}

.nav-bar ul {
    display: flex;
    /*Creates a flex container which will
    expand or shrink elements to fill the space available to them 
    and place the elements side by side horizontally*/
    list-style-type: none;
    /*Removes the default styling 
    for unordered lists elements, which is bullet points*/
    align-items: center;
    margin: -8px -8px 0px -8px;
    /*The margin order is top,
    right, bottom, left. The negative margins will stretch
    the background beyond the edge of the browser screen*/
    padding: 0;
    /*Removes default padding from the ul element*/
    background-color: rgb(1, 128, 106);
    /*Sets the background color of the navbar*/
    border-bottom: 5px solid rgb(247, 177, 188);
    /*Creates a border at the bottom of the navbar*/
}

a {
    display: block;
    /*Sets the display of the anchor links to block, which allows for padding and width to be applied to the links*/
    font-family: "Josefin Slab", serif;
    font-size: 25px;
    font-weight: 800;
    color: rgb(247, 177, 188);
    text-align: center;
    width: 100px;
    padding: 20px;
    text-decoration: none;
    /*Removes the default underline styling for links*/
    transition: background-color 0.3s ease-in;
    /*Adds a transition effect to the background color change when hovered over*/
}

a:hover,
.drop:hover a:only-of-type

/*Only-of-type effects
the only child element nested within an element. This will 
keep the background color blue while hovering and the text white 
when we move the mouse over anchor elements in the rollover-menu div*/
    {
    background-color: rgb(149, 238, 223);
    /*Changes the background color of the link when hovered over*/
    color: white;
    /*Changes the text color of the link when hovered over*/
    box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.7);
    /*Adds a shadow effect to the link when hovered over*/
    transition: background-color 0.3s ease-in;
}

/*Adds a transition effect to the background color change when hovered over*/

.drop {
    position: relative;
    /*Makes the dropdown menu position relative to this element*/
}

.rollover-menu {
    display: none;
    /*Prevents the anchor links in this div
    from showing on the web browser until otherwise
    specified*/
    position: absolute;
    /*Absolute will position the div
    element relative to the position of its parent element*/
    top: 100%;
    /*Positions the dropdown directly below the parent element*/
    left: 0;
    /*Aligns the dropdown with the left edge of the parent*/
    background-color: white;
    box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.2);
    /*Adds a shadow effect to the rollover menu*/
}

/*Creates a trigger effect when the user hovers their mouse
over the Recipes link. Trigger will display all elements
within the rollover-menu class div element*/
.drop:hover .rollover-menu {
    display: block;
}


.rollover-menu a {
    color: rgb(0, 80, 67);
}

.rollover-menu a:hover {
    color: white;
    box-shadow: 0px 8px 16px 0px rgba(90, 90, 90, 0.2);
    /*Adds a shadow effect to the link when hovered over*/
}

.rollover-menu a:nth-child(1) {
    border-top: 5px solid rgb(238, 118, 136);
    /*Adds a border to the top of the first link in the rollover menu*/
}

/*FOOTER*/
footer {
    align-items: center;
    /*Centers the content of the footer element vertically*/
    margin: 0px -8px -8px -8px;
    /*The margin order is top, right, bottom, left. The negative margins will stretch the background beyond the edge of the browser screen, leaving no white space*/
    background-color: rgb(1, 128, 106);
    border-top: 5px solid rgb(247, 177, 188);
    bottom: 0;
    /*Tells the browser to stick the element at the bottom of the page based on user scrolling*/
}

table {
    width: 100%;
    /*Sets the width of the table to fill the entire width of its container element*/
}

td {
    width: 33%;
    /*Sets the width of each table cell to 33% of the total width of the table, which allows for three cells to fit in each row*/
}

td p {
    font-family: "Josefin Slab", serif;
    /*Font from fonts.google.com*/
    font-size: 20px;
    font-weight: 600;
    text-align: center;
    color: rgb(247, 177, 188);
}

/*BODY*/

.container {
    display: block;
    background-size: 100%;
    padding-top: 60px;
    padding-left: 30px;
}

#recipes {
    clear: both;
    /*Clears floats from the featured recipe section above*/
}

/*HEADERS*/

h1 {
    font-family: "Sacramento", cursive;
    /*Font from fonts.google.com*/
    font-size: 80px;
    color: rgb(0, 80, 67);
    font-style: normal;
    text-align: center;
}

h2,
h3,
h4 {
    font-family: "Josefin Slab", serif;
    /*Font from fonts.google.com*/
    text-align: center;
}

h2 {
    color: rgb(0, 80, 67);
    font-size: 60px;
    text-decoration: underline;
    text-decoration-color: rgb(247, 177, 188);
}

h3 {
    font-size: 30px;
}

h4 {
    font-size: 15px;
    text-decoration: underline;
    text-transform: uppercase;
}

/*PARAGRAPHS*/

p {
    font-family: "Josefin Slab", serif;
    /*Font from fonts.google.com*/
    font-size: 20px;
    color: rgb(0, 80, 67);
    font-weight: 600;
    text-align: center;
}

/*IMAGES*/

/*Positions elements to sit side by side on the screen*/
.column {
    float: left;
    /*Floats the element to the left, allowing other elements to wrap around it*/
    width: 48%;
    /*Sets the width of each column to 48% of the total width of the container element, allowing for two columns to fit in each row*/
    padding: 10px;
    box-sizing: border-box;
    /*Includes padding in the width calculation so columns don't exceed 100% width*/
}

/*Styling for screens 1200px or smaller*/
@media screen and (max-width: 1200px) {
    .column {
        width: 100%;
        padding: 0px;
        margin-left: -15px;
        /*Centers each column on the screen*/
    }
}

/*Specifically styles the first 7 img tag elements contained
in elements with the .column class*/
.column img:nth-child(-n+7) {
    width: 500px;
    height: 350px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.4),
        0 6px 20px rgba(0, 0, 0, 0.2);
    /*Adds a shadow effect to the images*/
}

.recipe-container {
    width: 500px;
    height: 350px;
    margin: 100px auto 180px;
    /*Shorthand value top, right and left, 
    then bottom*/
    perspective: 1000px;
    /*Tells the browser how much 3D space an element should
    take up on the screen*/
}

.recipe-card-background {
    background-color: rgb(149, 238, 223);
}

/*Styling for mobile size screens*/
@media screen and (max-width: 600px) {
    .column img:nth-child(-n+7) {
        width: 350px;
        height: 225px;
    }

    .recipe-container {
        width: 350px;
        height: 225px;
    }
}

/*Alternates the blue tile background on smaller screen sizes*/
@media screen and (max-width: 1200px) {
    #pork {
        background-color: rgb(255, 255, 255);
    }

    #biscuits {
        background-color: rgb(149, 238, 223);
    }
}

/*FEATURED RECIPE*/

/*Styles the featured recipe h3 tag*/
#featured-header {
    font-family: 'Sacramento', cursive;
    /*Font from fonts.google.com*/
    font-size: 60px;
    color: rgb(0, 80, 67);
}

/*Styles the featured recipe main text*/
#featured-main-text {
    text-align: justify;
    padding-top: 20px;
    margin-left: 40px;
}

/*Styling for screens 1200px or smaller* Sets margin left to 0
because it is not needed on smaller screens and centers the text*/
@media screen and (max-width: 1200px) {
    #featured-main-text {
        margin-left: 0px;
    }
}

/*Styling for screens 1600px or larger* Increases the size of the featured recipe image*/
@media screen and (min-width: 1600px) {
    #featured img {
        width: 700px;
        height: auto;
        /*Automatically adjusts the height of the image to maintain its aspect ratio*/
        margin-left: -100px;
        margin-top: -20px;
        /*Margin left and top will override margin styling
        from the .recipe-container class and center the image on larger screens*/
    }
}

/*Adjusts the position of the featured recipe image on smaller screens by setting margin left to 0 and adding margin top to create space between the image and the text*/
@media screen and (max-width: 1599px) {
    #featured img {
        margin-left: 0px;
        margin-top: 40px;
        /*Margin top will override margin styling*/
    }
}

/*Styling for screens 600px or smaller* Decreases the height of the featured recipe image and adds margin top to create space between the image and the text on smaller screens*/
@media screen and (max-width: 600px) {
    #featured .recipe-card-background {
        height: 300px;
    }

    #featured img {
        margin-top: -65px;
    }
}


#featured-button {
    display: block;
    /*Positions the recipe button on its own line
    from the other elements in the featured recipe section*/
    margin: 60px auto 0px;
    /*Shorthand value top, right and left, then bottom. Centers the button on the screen*/
    background-color: rgb(1, 128, 106);
    border-radius: 35px;
    border: none;
    /*Removes the default border styling for button styling
    when border-radius is applied*/
    font-family: "Josefin Slab", serif;
    /*Font from fonts.google.com*/
    font-size: 30px;
    font-weight: bold;
    padding: 15px 25px;
    color: white;
    cursor: pointer;
}

/*Specifically styles the hover effect of the recipe button
in the Featured Recipe section*/
#featured-button:hover {
    background-color: rgb(247, 177, 188);
}

/*IMAGE FLIP*/

/*Positions the flip effect, defines transition time,
and defines a 3D effect*/
.flip {
    position: relative;
    width: 100%;
    height: 100%;
    /*Setting the width and height to 100%
    will allow the flip effect to fill the entire area of the recipe card*/
    text-align: center;
    transition: 0.5s ease-in-out;
    transform-style: preserve-3d;
    /*Treats elements as 3D objects
    instead of flat (2D) objects*/
}

.flip:hover {
    transform: rotateY(180deg);
    /*Rotates the element 180 degrees on the Y-axis when hovered over, creating a flip effect*/
}

.flip-front,
.flip-back {
    position: absolute;
    width: 100%;
    height: 100%;
    backface-visibility: hidden;
}

.flip-back {
    background-color: rgb(149, 238, 223);
    border: 2px solid white;
    transform: rotateY(180deg);
    /*Adds 180 degrees back from 
    the previously subtracted -180 degrees it moved to previously
    so the text can be read properly*/
}


.text-container {
    padding-top: 70px;
}

@media screen and (max-width: 600px) {
    .text-container {
        padding-top: 0px;
    }
}

/*RECIPE BUTTONS*/

input[type="button"] {
    background-color: #fff;
    border: none;
    border-radius: 20px;
    font-family: "Josefin Slab", serif;
    /*Font from fonts.google.com*/
    font-weight: bold;
    font-size: 22px;
    padding: 11px 17px;
    color: rgb(1, 128, 106);
    cursor: pointer;
}

input[type="button"]:hover {
    background-color: rgb(247, 177, 188);
    color: white;
}

/*CONTACT FORM CSS*/

/*Sets up the content within the div tag containing the ID 
contact to be centered on the screen*/
#contact {
    clear: both;
    display: block;
    margin-bottom: 70px;
}

form {
    display: table;
    /*Sets the form element to behave like a table, allowing for the use of table properties to align form elements*/
    padding: 15px;
    margin-left: auto;
    margin-right: auto;
    /*Centers the form element on the screen by setting left and right margins to auto*/
}

/*This will specifically target the input tags that are
nested within the form tags*/
form input {
    font-family: 'Courier New', Courier, monospace;
    font-size: 15px;
    color: rgb(165, 165, 165);
    border: 2px solid rgb(165, 165, 165);
    border-radius: 5px;
    padding: 3px;
    margin: 10px 15px 10px 0px;
    transition: 0.3s ease-out;
    /*Adds a transition effect to the border and text color change when the input field is focused*/
}

form input:focus {
    border: 2px solid rgb(1, 128, 106);
    color: black;
    outline: none;
    /*Removes the default blue outline styling for focused input elements*/
    transition: 0.3s ease-out;
}

label {
    font-family: "Josefin Slab", serif;
    /*Font from fonts.google.com*/
    font-size: 20px;
    font-weight: bold;
    margin: 10px;
}

textarea {
    width: 100%;
    height: 200px;
    padding: 12px 20px;
    box-sizing: border-box;
    resize: both;
    /*Allows the user to resize the textarea both horizontally and vertically*/
    border: 3px solid rgb(165, 165, 165);
    border-radius: 5px;
    font-family: 'Courier New', Courier, monospace;
    color: rgb(165, 165, 165);
    font-size: 15px;
    transition: 0.3s ease-out;
    /*Adds a transition effect to the border and text color change when the textarea is focused*/
}

textarea:focus {
    color: black;
    border: 3px solid rgb(1, 128, 106);
    transition: 0.3s ease-out;
    outline: none;
}

input[type="submit"] {
    background-color: rgb(1, 128, 106);
    border: none;
    border-radius: 35px;
    font-family: "Josefin Slab", serif;
    /*Font from fonts.google.com*/
    font-weight: bold;
    font-size: 20px;
    padding: 15px 25px;
    color: #fff;
    cursor: pointer;
    float: right;
    /*Floats the submit button to the right, allowing it to sit on the same line as the textarea element*/
}

input[type="submit"]:hover {
    background-color: rgb(247, 177, 188);
}

/*ABOUT US section styling*/
#about {
    padding: 70px 30px;
    clear: both;
    display: block;
    scroll-margin-top: 100px;
}

#about p {
    text-align: justify;
}

/*RECIPE MODAL*/
.recipe-modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    background-color: rgba(255, 255, 255, 0.5);
    /*Creates a semi-transparent white background for the modal*/
    width: 100%;
    height: 100%;
}

.recipe-content {
    position: relative;
    background-color: rgb(255, 255, 255);
    top: 15vh;
    left: 27vw;
    width: 45vw;
    height: 70%;
    box-shadow: 0 2px 10px 5px rgba(0, 0, 0, 0.2);
    box-sizing: border-box;
    overflow-y: auto;
}

.close-btn {
    color: rgb(1, 128, 106);
    font-size: 50px;
    font-weight: 600;
    cursor: pointer;
    position: absolute;
    top: 10px;
    right: 18px;
    line-height: 1;
}

.modal-header {
    padding-top: 20px;
}

.modal-body {
    padding: 20px;
}

.row {
    display: flex;
    justify-content: center;
}

.rp-column-1 {
    width: 33%;
}

.rp-column-2 {
    width: 67%;
}

.recipe-list {
    margin-left: -30px;
    font-family: "Josefin Slab", serif;
    font-size: 18px;
    display: block; /*Positions the recipe list on its own line from the other elements in the modal. Overrides the default inline display of the unordered list element*/
    border: none; /*Removes the default border styling for unordered list elements*/
    background-color: transparent; /*Removes the default background styling for unordered list elements*/
    line-height: 1.5;
    text-align: center;
}

.recipe-directions {
    padding: 0px 20px;
    font-size: 18px;
    font-weight: 500;
    text-align: left;
}

.modal-body ul {
    display: block;
    list-style-type: disc;
    margin: 0;
    padding-left: 20px;
    background-color: transparent;
    border: none;
}

.modal-body li {
    display: list-item;
    font-family: "Josefin Slab", serif;
    font-size: 18px;
    color: rgb(0, 80, 67);
    line-height: 1.5;
    text-align: left;
}


